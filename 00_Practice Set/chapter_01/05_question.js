//Predict the output of the following:

console.log("5" + 5);   // "55"   → string concatenation
console.log("5" - 2);   // 3      → "5" gets converted to number
console.log(true + true); // 2    → true = 1, so 1 + 1 = 2
console.log(false + 10); // 10    → false = 0, so 0 + 10 = 10
